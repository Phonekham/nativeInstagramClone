const {StyleSheet} = require('react-native');

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  name: {
    fontWeight: 'bold',
    alignSelf: 'center',
    color: '#3c3c3c',
  },
});

export default styles;
